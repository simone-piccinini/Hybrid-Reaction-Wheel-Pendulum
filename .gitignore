# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Virtual environment
.venv/

# Testing
.pytest_cache/

# Editors / IDEs
.theia/
.vscode/
.idea/

# macOS
.DS_Store

from_last_time.txt

# Local implementation notes — kept off GitHub on purpose
docs/implementation_notes/

# NOTE: results/ is intentionally NOT ignored. Experiment run records (config,
# seed, git hash, metrics, trajectories, plots) are the evidence behind every
# number quoted in docs/, so they are version controlled alongside the raw
# hardware captures in data/hw/.
