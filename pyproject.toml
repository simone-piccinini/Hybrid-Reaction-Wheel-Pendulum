[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "inverted-pendulum"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["core*"]     # ← explicitly include the core package